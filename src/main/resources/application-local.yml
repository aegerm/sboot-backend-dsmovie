spring:
  datasource:
    url: jdbc:h2:mem:db_movie_local
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true